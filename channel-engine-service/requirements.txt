# FastAPI and server
# 上界钉死 (2026-08-02, 与 api-service 同因): 0.141 起 include_router 改懒加载,
# 一次静默升级打瞎了 api-service 的路由枚举门。本服务今天没有同类门, 但
# "开放上界 = 静默升级"的根因对两个服务对称成立, 结论同步落地。
fastapi>=0.115.0,<0.142.0
uvicorn[standard]>=0.34.0
pydantic>=2.0.0

# CORS middleware
python-multipart>=0.0.9

# ChannelEngine dependencies (inherit from ChannelEgine)
numpy>=1.24.0
scipy>=1.10.0

# Development and testing
pytest>=8.0.0
httpx>=0.27.0

# Channel Engine UI & Plotting
streamlit>=1.52.0
matplotlib>=3.8.0
