# FastAPI and server
# 上界钉死 (2026-08-02): 0.141 起 include_router 改懒加载 (_IncludedRouter),
# app.routes 不再展平 —— 一次静默升级当场打瞎 test_rule_gates 的路由取数,
# G6/G8/G11 三门同时假红且归因错误。取数器已换成递归展开 + openapi 交叉自检
# (见 _live_route_table), 0.141.1 实测全绿; 再放次版本前先跑规则门。
# ⚠️ starlette 是传递依赖**未钉** (fastapi 0.141.1 只声明 starlette>=0.46.0,
# 无上界), 而 Route/methods 这些路由模型字段在 starlette 里 —— 本行上界
# **挡不住** starlette 单独跳版重演同类结构变更。那一侧的保护靠
# _live_route_table 的集合包含自检 (会红并指出取数源失效), 不靠版本号。
fastapi>=0.115.0,<0.142.0
uvicorn[standard]>=0.34.0
pydantic>=2.4.0  # json_schema_serialization_defaults_required entered ConfigDict in 2.4
pydantic-settings>=2.0.0
tzdata>=2025.2

# Database
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0
alembic>=1.13.0

# CORS middleware
python-multipart>=0.0.9

# Authentication
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# PDF generation (for certificates)
reportlab>=4.0.0
pypdf>=3.17.0

# Template rendering and chart generation
jinja2>=3.1.0
matplotlib>=3.8.0
pillow>=10.0.0

# Utilities
python-dateutil>=2.8.0
numpy>=2.0.0  # rs_fsva IQ 解析依赖 np.fromstring 坏数据 raise（1.x 静默截断）
scipy>=1.11.0
pyyaml>=6.0

# VISA Instrument Communication
pyvisa>=1.14.0           # Python bindings for NI-VISA
pyvisa-py>=0.7.0         # Pure-Python VISA backend (fallback when NI-VISA not installed)

# Development and testing
pytest>=8.0.0
httpx>=0.27.0
pytest-asyncio>=0.23.0

# Channel Engine UI
streamlit>=1.52.0
