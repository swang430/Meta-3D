# ==================== 系统校准 Mock 模式配置 ====================
#
# 控制系统校准 API 的 Mock 行为
#
# 可选值:
#   - auto (默认): 自动模式 - 优先使用真实 API Service (http://localhost:8001)
#                  如果连接失败，自动降级到 Mock 数据
#                  适用于: 开发环境（API Service 可能未启动）
#
#   - force: 强制 Mock 模式 - 总是使用 Mock 数据，不调用真实 API
#            适用于: 离线开发、UI 演示、前端独立测试
#
#   - disabled: 禁用 Mock - 只使用真实 API，连接失败时报错
#               适用于: 生产环境、集成测试
#
# 使用方法:
#   1. 复制此文件为 .env
#   2. 取消注释并设置所需的模式
#   3. 重启开发服务器 (npm run dev)
#
# VITE_CALIBRATION_MOCK_MODE=auto
# VITE_CALIBRATION_MOCK_MODE=force
# VITE_CALIBRATION_MOCK_MODE=disabled

# ==================== 示例使用场景 ====================
#
# 场景 1: 开发阶段，API Service 未启动
#   VITE_CALIBRATION_MOCK_MODE=auto  （或不设置，使用默认值）
#   结果: 前端自动使用 Mock 数据，无需启动后端
#
# 场景 2: 演示/展示，完全离线
#   VITE_CALIBRATION_MOCK_MODE=force
#   结果: 保证使用 Mock，无网络请求
#
# 场景 3: 端到端测试，必须连接真实后端
#   VITE_CALIBRATION_MOCK_MODE=disabled
#   结果: 强制使用真实 API，失败时报错（便于发现问题）
